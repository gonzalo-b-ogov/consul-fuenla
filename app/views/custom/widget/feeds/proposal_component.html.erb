<div class="feed-content-proposals-home">
<div class="proposal col-lg-3 col-md-6 col-xs-12">
  <div class="feed-description small-12 column
             <%= "medium-6 large-9" if proposal.image.present? %>">
    <strong><%= link_to proposal.title, url_for(proposal) %></strong><br>
    <p><%= proposal.summary %></p>
    <%= render SDG::TagListComponent.new(proposal, limit: 5) %>
  </div>

  <% if proposal.image.present? %>
    <div class="small-12 column">
      <div class="feed-image">
        <%= image_tag proposal.image.variant(:thumb),
                      alt: proposal.image.title.unicode_normalize %>
      </div>
    </div>
  <% end %>
</div>
</div>